# Samples of using racket's ffi interface

```
$ ./test.sh
```
# Notes

- For some reason, click `run` in drracket doesn't work for ffi
- Use raco's ctool?
- Use plain makefile?

## list symbols from a lib

`nm -g ./librac.* `